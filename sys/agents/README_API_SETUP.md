# Настройка API ключа для RAG Q/A системы

## 🔑 3 способа добавить API ключ

### ✅ Способ 1: Через .env файл (РЕКОМЕНДУЕТСЯ)

1. Создайте файл `.env` в папке `sys/agents/`:
```bash
cd "c:\Users\acernitro\OneDrive\Рабочий стол\nothing_startup\sys\agents"
copy .env.example .env
```

2. Откройте `.env` и добавьте ваш ключ:
```
OPENAI_API_KEY=sk-ваш-реальный-ключ-здесь
MODEL_NAME=gpt-3.5-turbo
TEMPERATURE=0.7
MAX_TOKENS=500
```

3. Запустите систему:
```bash
python qa_rag_with_llm.py
```

### Способ 2: Через переменные окружения

**Windows:**
```cmd
set OPENAI_API_KEY=sk-ваш-ключ
python qa_rag_with_llm.py
```

**Linux/Mac:**
```bash
export OPENAI_API_KEY=sk-ваш-ключ
python qa_rag_with_llm.py
```

### Способ 3: Прямо в коде (НЕ БЕЗОПАСНО)

Откройте `qa_rag_with_llm.py` и найдите строку 38:
```python
# OPENAI_API_KEY = "sk-your-api-key-here"
```

Раскомментируйте и вставьте ваш ключ:
```python
OPENAI_API_KEY = "sk-ваш-реальный-ключ"
```

⚠️ **ВАЖНО:** Не коммитьте этот файл в Git!

## 🔗 Где получить API ключ

### OpenAI API
1. Перейдите на https://platform.openai.com/api-keys
2. Нажмите "Create new secret key"
3. Скопируйте ключ (он начинается с `sk-`)
4. Сохраните в `.env` файл

### Anthropic API (Claude)
1. Перейдите на https://console.anthropic.com/
2. Создайте API ключ
3. Добавьте в `.env`:
```
ANTHROPIC_API_KEY=ваш-ключ
```

## 📦 Установка зависимостей

Для работы с OpenAI:
```bash
pip install python-dotenv langchain-openai
```

## 🚀 Запуск

**С LLM (если есть API ключ):**
```bash
python qa_rag_with_llm.py
```

**Без LLM (простой поиск):**
```bash
python qa_rag.py
```

## 📝 Примеры использования

После запуска задавайте вопросы:
```
[Q/A] Ваш вопрос: Что такое искусственный интеллект?
[Q/A] Ваш вопрос: Расскажи про машинное обучение
[Q/A] Ваш вопрос: история  (показать историю)
[Q/A] Ваш вопрос: выход  (завершить)
```

## 🛠️ Структура файлов

```
sys/agents/
├── .env                     # ВАШ API ключ (создайте этот файл!)
├── .env.example            # Пример конфигурации
├── qa_rag.py              # Простая версия БЕЗ LLM
├── qa_rag_with_llm.py     # Версия С LLM
├── rag.py                 # Базовый RAG агент
├── data/                  # Положите сюда .txt файлы
│   └── example.txt
└── README_API_SETUP.md    # Эта инструкция
```

## ❓ Проблемы

### "API ключ не найден"
- Проверьте, что файл `.env` создан в правильной папке
- Проверьте, что ключ начинается с `sk-`
- Проверьте, что нет лишних пробелов в `.env`

### "ModuleNotFoundError: No module named 'langchain_openai'"
```bash
pip install langchain-openai python-dotenv
```

### Работает без LLM
- Система автоматически переключится на простой режим, если нет API ключа
- Это нормально! Просто добавьте ключ для улучшенных ответов

## 💡 Совет

Начните с `qa_rag.py` (без API ключа), чтобы протестировать систему.
Затем добавьте API ключ и переключитесь на `qa_rag_with_llm.py` для умных ответов.
